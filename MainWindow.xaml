<Window x:Class="AudioTranscriber.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AudioTranscriber.ViewModels"
        mc:Ignorable="d"
        Title="语音转写姬" 
        Height="200" Width="600"
        MinHeight="100" MinWidth="400"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        Topmost="True"
        Foreground="#FFE0E0E0"
        FontFamily="Microsoft YaHei, SimHei, sans-serif"
        ResizeMode="CanResizeWithGrip"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseDoubleClick="Window_MouseDoubleClick">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- 设置按钮样式 -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Opacity" Value="0.7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 字幕项样式 -->
        <DataTemplate x:Key="SubtitleItemTemplate">
            <StackPanel Margin="0,2">
                <!-- 原文 -->
                <TextBlock Text="{Binding OriginalText}"
                           Foreground="{Binding DataContext.SubtitleColor, RelativeSource={RelativeSource AncestorType=Window}}"
                           FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource AncestorType=Window}}"
                           FontWeight="Bold"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Effect="{Binding DataContext.TextShadowEffect, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <!-- 翻译 -->
                <TextBlock Text="{Binding TranslatedText}"
                           Foreground="{Binding DataContext.TranslationColor, RelativeSource={RelativeSource AncestorType=Window}}"
                           FontSize="{Binding DataContext.TranslationFontSize, RelativeSource={RelativeSource AncestorType=Window}}"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,4,0,0"
                           Visibility="{Binding IsEnglish, Converter={StaticResource BooleanToVisibilityConverter}}"
                           Effect="{Binding DataContext.TextShadowEffect, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </DataTemplate>

        <!-- 透明滚动条样式 -->
        <Style x:Key="TransparentScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                      Grid.Column="1"
                                      Orientation="Vertical"
                                      Value="{TemplateBinding VerticalOffset}"
                                      Maximum="{TemplateBinding ScrollableHeight}"
                                      ViewportSize="{TemplateBinding ViewportHeight}"
                                      Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                      Background="Transparent"
                                      Width="6"
                                      Margin="4,0,0,0">
                                <ScrollBar.Style>
                                    <Style TargetType="ScrollBar">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ScrollBar">
                                                    <Grid>
                                                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                                                            <Track.Thumb>
                                                                <Thumb>
                                                                    <Thumb.Template>
                                                                        <ControlTemplate>
                                                                            <Border Background="#55FFFFFF"
                                                                                   CornerRadius="3"
                                                                                   Width="6"/>
                                                                        </ControlTemplate>
                                                                    </Thumb.Template>
                                                                </Thumb>
                                                            </Track.Thumb>
                                                        </Track>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ScrollBar.Style>
                            </ScrollBar>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 设置面板阴影效果 -->
        <DropShadowEffect x:Key="MaterialDesignShadowDepth3"
                         ShadowDepth="8"
                         Direction="270"
                         Color="#FF000000"
                         Opacity="0.6"
                         BlurRadius="20"/>
    </Window.Resources>

    <Grid>
        <!-- 主字幕区域 -->
        <Border x:Name="SubtitleContainer"
                Background="{Binding BackgroundBrush}"
                CornerRadius="{Binding CornerRadius}"
                BorderBrush="{Binding BorderBrush}"
                BorderThickness="{Binding BorderThickness}"
                Padding="20,15"
                Margin="10">
            <Grid>
                <!-- 字幕列表 -->
                <ScrollViewer x:Name="SubtitleScrollViewer"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled"
                             Style="{StaticResource TransparentScrollViewer}">
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding TranscriptSegments}"
                                     ItemTemplate="{StaticResource SubtitleItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- 空状态提示 -->
                <TextBlock Text="🎤 等待语音输入..." 
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Foreground="#88FFFFFF"
                          FontSize="16"
                          Opacity="0.5">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding TranscriptSegments.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <!-- 右上角控制按钮 -->
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Right"
                           VerticalAlignment="Top"
                           Margin="0,-5,-5,0">
                    <!-- 录制状态指示器 -->
                    <Ellipse Width="8" Height="8" Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#FF4444"/>
                                <Setter Property="Opacity" Value="0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentState}" Value="Recording">
                                        <Setter Property="Opacity" Value="1"/>
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                   From="1" To="0.3" Duration="0:0:0.5"
                                                                   AutoReverse="True"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    
                    <!-- 设置按钮 -->
                    <Button x:Name="SettingsButton"
                           Style="{StaticResource IconButton}"
                           Content="⚙️"
                           Click="SettingsButton_Click"
                           ToolTip="设置"/>
                    
                    <!-- 关闭按钮 -->
                    <Button Style="{StaticResource IconButton}"
                           Content="✕"
                           Margin="8,0,0,0"
                           Click="CloseButton_Click"
                           ToolTip="关闭"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 设置面板 (默认隐藏) - 实体背景，非透明 -->
        <Border x:Name="SettingsPanel"
                Background="#FF1E1E2E"
                Width="450"
                Height="550"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="Collapsed"
                Effect="{DynamicResource MaterialDesignShadowDepth3}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 设置标题栏 -->
                <Border Grid.Row="0" 
                        Background="#FF2D2D44" 
                        Padding="20,15">
                    <Grid>
                        <TextBlock Text="⚙️ 设置" 
                                  FontSize="20" 
                                  FontWeight="Bold"
                                  Foreground="White"
                                  VerticalAlignment="Center"/>
                        <Button Style="{StaticResource IconButton}"
                               Content="✕"
                               HorizontalAlignment="Right"
                               Click="CloseSettings_Click"
                               FontSize="18"/>
                    </Grid>
                </Border>
                
                <!-- 设置内容 -->
                <ScrollViewer Grid.Row="1" 
                             VerticalScrollBarVisibility="Auto"
                             Margin="20"
                             Style="{StaticResource TransparentScrollViewer}">
                    <StackPanel>
                        <!-- 音频源 -->
                        <TextBlock Text="🎧 音频源" Foreground="#FFAAAAAA" FontSize="12" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding AudioDevices}"
                                 SelectedItem="{Binding SelectedDevice}"
                                 DisplayMemberPath="Name"
                                 Background="#22FFFFFF"
                                 Foreground="Black"
                                 BorderBrush="#44FFFFFF"
                                 Padding="8,5"
                                 Margin="0,0,0,15"/>
                        
                        <!-- 翻译服务 -->
                        <TextBlock Text="🌐 翻译服务" Foreground="#FFAAAAAA" FontSize="12" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding TranslationProviders}"
                                 SelectedItem="{Binding SelectedTranslationProvider}"
                                 DisplayMemberPath="Name"
                                 Background="#22FFFFFF"
                                 Foreground="Black"
                                 BorderBrush="#44FFFFFF"
                                 Padding="8,5"
                                 Margin="0,0,0,5"/>
                        <Button Content="🔄 刷新Ollama模型"
                               Command="{Binding RefreshOllamaModelsCommand}"
                               Background="#33FFFFFF"
                               Foreground="White"
                               BorderThickness="0"
                               Padding="8,5"
                               Margin="0,0,0,15"
                               HorizontalAlignment="Left"/>
                        
                        <!-- 字幕样式 -->
                        <TextBlock Text="🎨 字幕样式" Foreground="#FFAAAAAA" FontSize="12" Margin="0,0,0,10"/>
                        
                        <!-- 字体大小 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="字体大小:" Foreground="#FFCCCCCC" Grid.Column="0" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" 
                                   Minimum="12" Maximum="48" 
                                   Value="{Binding FontSize}"
                                   Margin="10,0"
                                   VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding FontSize}" Foreground="White" Grid.Column="2" Width="25"/>
                        </Grid>
                        
                        <!-- 透明度 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="背景透明:" Foreground="#FFCCCCCC" Grid.Column="0" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" 
                                   Minimum="0" Maximum="100" 
                                   Value="{Binding BackgroundOpacity}"
                                   Margin="10,0"
                                   VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding BackgroundOpacity, StringFormat='{}{0}%'}" Foreground="White" Grid.Column="2" Width="35"/>
                        </Grid>
                        
                        <!-- 背景颜色 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="背景颜色:" Foreground="#FFCCCCCC" Grid.Column="0" VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="1"
                                     SelectedItem="{Binding SelectedBackgroundColor}"
                                     Margin="10,0,0,0"
                                     Background="#22FFFFFF"
                                     Foreground="Black">
                                <ComboBoxItem Content="黑色" Tag="#000000"/>
                                <ComboBoxItem Content="深蓝" Tag="#1A1A2E"/>
                                <ComboBoxItem Content="深紫" Tag="#2D1B4E"/>
                                <ComboBoxItem Content="深绿" Tag="#1B3A2E"/>
                                <ComboBoxItem Content="深红" Tag="#3A1B1B"/>
                                <ComboBoxItem Content="透明" Tag="#000000"/>
                            </ComboBox>
                        </Grid>
                        
                        <!-- 圆角 -->
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="圆角大小:" Foreground="#FFCCCCCC" Grid.Column="0" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" 
                                   Minimum="0" Maximum="50" 
                                   Value="{Binding CornerRadius}"
                                   Margin="10,0"
                                   VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding CornerRadius}" Foreground="White" Grid.Column="2" Width="25"/>
                        </Grid>
                        
                        <!-- 其他选项 -->
                        <CheckBox Content="启用翻译" 
                                 IsChecked="{Binding EnableTranslation}"
                                 Foreground="White"
                                 Margin="0,0,0,10"/>
                        
                        <CheckBox Content="自动保存字幕" 
                                 IsChecked="{Binding AutoSave}"
                                 Foreground="White"
                                 Margin="0,0,0,10"/>
                        
                        <!-- 保存路径 -->
                        <TextBlock Text="📁 保存路径:" Foreground="#FFAAAAAA" FontSize="12" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding SaveDirectory}" 
                                  Foreground="#FFCCCCCC"
                                  FontSize="11"
                                  TextTrimming="CharacterEllipsis"
                                  Margin="0,0,0,5"/>
                        <Button Content="更改保存路径"
                               Command="{Binding ChangeSaveDirectoryCommand}"
                               Background="#33FFFFFF"
                               Foreground="White"
                               BorderThickness="0"
                               Padding="8,5"
                               HorizontalAlignment="Left"/>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- 底部按钮区域 -->
                <Border Grid.Row="2" Background="#FF2D2D44" Padding="20,15">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="🔴 开始录音"
                               Command="{Binding StartRecordingCommand}"
                               Background="#FFE53935"
                               Foreground="White"
                               BorderThickness="0"
                               Padding="20,10"
                               Margin="5,0"
                               FontWeight="Bold">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentState}" Value="Idle">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="⏹️ 停止录音"
                               Command="{Binding StopRecordingCommand}"
                               Background="#FF757575"
                               Foreground="White"
                               BorderThickness="0"
                               Padding="20,10"
                               Margin="5,0"
                               FontWeight="Bold">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentState}" Value="Recording">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="🗑️ 清空"
                               Command="{Binding ClearTranscriptCommand}"
                               Background="#FF5C6BC0"
                               Foreground="White"
                               BorderThickness="0"
                               Padding="20,10"
                               Margin="5,0"
                               FontWeight="Bold"/>

                        <Button Content="💾 保存"
                               Command="{Binding SaveTranscriptCommand}"
                               Background="#FF43A047"
                               Foreground="White"
                               BorderThickness="0"
                               Padding="20,10"
                               Margin="5,0"
                               FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>